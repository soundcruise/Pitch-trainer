/**
 * Pro版「入室」用の4桁パスワードを設定します。
 *
 * 使い方: このファイルを `gate-config.js` にコピーし、下の4桁を変更してください。
 * `gate-config.js` は Git にコミットしない運用を推奨します（URL_manual/URL_manual.txt 参照）。
 */
window.__PRO_GATE_PASSWORD__ = '0000';
